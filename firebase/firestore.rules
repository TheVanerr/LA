rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    match /date_responses/{sessionId} {
      // Site sadece yazabilsin; okuma kapalı (Firebase Console'dan okursun).
      allow read: if false;
      allow create, update: if true;
      allow delete: if false;
    }
  }
}
